{
  "comment": "Windows configuration running the published package inside WSL.",
  "comment2": "Requires uv installed in the WSL distro, not on Windows.",
  "comment3": "'wsl.exe -- uvx' runs a login-less shell, so use the absolute uvx path",
  "comment4": "if uvx is not found - typically /home/<user>/.local/bin/uvx.",
  "mcpServers": {
    "pal": {
      "command": "wsl.exe",
      "args": ["--", "uvx", "pal-mcp-server"],
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY}"
      }
    }
  }
}
