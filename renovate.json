{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", ":semanticCommits"],
  "forkProcessing": "enabled",
  "labels": ["dependencies"],
  "pre-commit": {
    "enabled": true
  },
  "packageRules": [
    {
      "description": "uv is pinned in three places that must move together (pyproject required-version, setup-uv version, this image); bump them by hand, see CLAUDE.md.",
      "matchPackageNames": ["ghcr.io/astral-sh/uv"],
      "enabled": false
    }
  ]
}
