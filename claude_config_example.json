{
  "comment": "Example MCP client configuration for PAL MCP Server (published on PyPI).",
  "comment2": "uvx fetches and runs the package; no clone, no virtualenv to maintain.",
  "comment3": "Set the API keys in your own environment - ${VAR} is expanded by the client.",
  "comment4": "For platform-specific examples, see the examples/ directory.",
  "mcpServers": {
    "pal": {
      "command": "uvx",
      "args": ["pal-mcp-server"],
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY}"
      }
    }
  }
}
